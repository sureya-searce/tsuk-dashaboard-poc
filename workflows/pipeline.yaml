# Cloud Workflow — orchestrates the full PoC pipeline.
#
# Triggered by Eventarc on `google.cloud.storage.object.v1.finalized` from the
# landing bucket. Eventarc has no native prefix filter, so this workflow filters
# at step 1: the object MUST be at:
#
#     landing/<feed>/<filename>.xlsx
#
# where <feed> is one of the allowed feeds (rail, road_uk, road_eu,
# finance_prodcost, finance_mgmt). Anything else short-circuits with status SKIPPED.
#
# GCP Workflows requires $${...} expression values to be single-quoted.

main:
  params: [event]
  steps:
    - init:
        assign:
          - project_id: "${project_id}"
          - region: "${region}"
          - raw_dataset: "${raw_dataset}"
          - stg_dataset: "${stg_dataset}"
          - core_dataset: "${core_dataset}"
          - finance_dataset: "${finance_dataset}"
          - sc_dataset: "${sc_dataset}"
          - ingest_url: "${ingest_url}"
          # Looker cache warm — deferred until Looker access lands.
          # - warm_url: (set from warm Cloud Run service when re-enabled)
          - landing_prefix: "${landing_prefix}"
          - allowed_feeds: ${allowed_feeds}
          - bucket: '$${event.data.bucket}'
          - object: '$${event.data.name}'

    # Path must be exactly: landing/<feed>/<file>.xlsx
    # Use text.split (robust) rather than regex capture groups, which the
    # Workflows text functions do not expose.
    - parse_path:
        assign:
          - parts: '$${text.split(object, "/")}'

    - guard_structure:
        switch:
          - condition: '$${len(parts) != 3 or parts[0] != "landing" or not text.match_regex(parts[2], "[.]xlsx$")}'
            next: skip_bad_path
        next: assign_parts

    - assign_parts:
        assign:
          - feed: '$${parts[1]}'
          - file_name: '$${parts[2]}'

    - guard_feed:
        switch:
          - condition: '$${not (feed in allowed_feeds)}'
            next: skip_unknown_feed
        next: ingest

    - skip_bad_path:
        return:
          status: SKIPPED
          reason: '$${"Object path does not match landing feed file pattern: " + object}'

    - skip_unknown_feed:
        return:
          status: SKIPPED
          reason: '$${"Feed not in allowed list: " + feed}'
          object: '$${object}'

    - ingest:
        call: http.post
        args:
          url: '$${ingest_url + "/ingest"}'
          auth:
            type: OIDC
            audience: '$${ingest_url}'
          body:
            bucket: '$${bucket}'
            object: '$${object}'
            feed: '$${feed}'
          timeout: 540
        result: ingest_result

    # Silver — normalise the logistics feeds, then the finance feeds.
    - normalise:
        call: googleapis.bigquery.v2.jobs.query
        args:
          projectId: '$${project_id}'
          body:
            useLegacySql: false
            location: EU
            timeoutMs: 60000
            query: '$${"CALL `" + project_id + "." + stg_dataset + ".sp_normalise`()"}'
        result: normalise_result

    - finance_stage:
        call: googleapis.bigquery.v2.jobs.query
        args:
          projectId: '$${project_id}'
          body:
            useLegacySql: false
            location: EU
            timeoutMs: 60000
            query: '$${"CALL `" + project_id + "." + stg_dataset + ".sp_finance_stage`()"}'
        result: finance_stage_result

    # Gold core — conformed facts + the governed cross-team KPI.
    - build_core:
        call: googleapis.bigquery.v2.jobs.query
        args:
          projectId: '$${project_id}'
          body:
            useLegacySql: false
            location: EU
            timeoutMs: 60000
            query: '$${"CALL `" + project_id + "." + core_dataset + ".sp_build_core`()"}'
        result: build_core_result

    # Gold team marts — Finance-owned, then Supply-Chain-owned.
    - build_finance:
        call: googleapis.bigquery.v2.jobs.query
        args:
          projectId: '$${project_id}'
          body:
            useLegacySql: false
            location: EU
            timeoutMs: 60000
            query: '$${"CALL `" + project_id + "." + finance_dataset + ".sp_build_finance`()"}'
        result: build_finance_result

    - build_supplychain:
        call: googleapis.bigquery.v2.jobs.query
        args:
          projectId: '$${project_id}'
          body:
            useLegacySql: false
            location: EU
            timeoutMs: 60000
            query: '$${"CALL `" + project_id + "." + sc_dataset + ".sp_build_supplychain`()"}'
        result: build_supplychain_result

    # Shared receipts — reconciliation + DQ across logistics and finance.
    - data_quality:
        call: googleapis.bigquery.v2.jobs.query
        args:
          projectId: '$${project_id}'
          body:
            useLegacySql: false
            location: EU
            timeoutMs: 60000
            query: '$${"CALL `" + project_id + "." + core_dataset + ".sp_data_quality`()"}'
        result: dq_result

    # ── Looker cache warm — DEFERRED until Looker access lands ──────────
    # Uncomment this step (and warm_url in init, warm_result in done, and the
    # warm Cloud Run service in infra/cloudrun.tf) once Looker is reachable.
    # - warm_cache:
    #     try:
    #       call: http.post
    #       args:
    #         url: '$${warm_url + "/warm"}'
    #         auth:
    #           type: OIDC
    #           audience: '$${warm_url}'
    #         body:
    #           dashboards: ["finance", "logistics", "anomaly"]
    #         timeout: 120
    #       result: warm_result
    #     except:
    #       as: warm_error
    #       steps:
    #         - capture_warm_error:
    #             assign:
    #               - warm_result:
    #                   status: WARN
    #                   error: '$${warm_error}'

    - done:
        return:
          status: OK
          feed: '$${feed}'
          file: '$${file_name}'
          ingest: '$${ingest_result.body}'
          normalise_job: '$${normalise_result.jobReference}'
          finance_stage_job: '$${finance_stage_result.jobReference}'
          build_core_job: '$${build_core_result.jobReference}'
          build_finance_job: '$${build_finance_result.jobReference}'
          build_supplychain_job: '$${build_supplychain_result.jobReference}'
          data_quality_job: '$${dq_result.jobReference}'
          # warm: '$${warm_result}'   # deferred — see warm_cache above
